{
  "name": "esri-gatsby",
  "version": "1.0.0",
  "private": true,
  "description": "An example of how to use the ArcGIS API for JavaScript in a Gatsby application via esri-loader-hooks.",
  "keywords": [
    "gatsby",
    "esri",
    "ArcGIS",
    "esri-loader",
    "esri-loader-hooks"
  ],
  "scripts": {
    "develop": "gatsby develop",
    "start": "gatsby develop",
    "build": "gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "deploy": "gatsby build --prefix-paths && gh-pages -d public"
  },
  "dependencies": {
    "esri-loader": "^3.0.0",
    "esri-loader-hooks": "^0.2.0",
    "gatsby": "^2.31.1",
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
